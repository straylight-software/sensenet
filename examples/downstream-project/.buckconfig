# Buck2 Configuration for downstream project
#
# This is the minimum required .buckconfig for a project using sensenet.
# Toolchain paths are set in .buckconfig.local (generated by nix develop).

[cells]
root = .
prelude = nix/build/prelude
toolchains = nix/build/toolchains

[parser]
target_platform_detector_spec = target:root//...->prelude//platforms:default

[buck2]
materializations = deferred
digest_algorithms = BLAKE3

[build]
execution_platforms = toolchains//:local
