result
result-*
.direnv/
buck-out/
.buckconfig.local
nix/build/
bin/

# sense CLI binary (built from src/sense/)
/sense
dist-newstyle/

# Compiled binaries (should never be tracked)
/bench-main
/bench/dhall-fast-bench
/bench/integration-test
/sensenet-stage1
/sensenet-test
/test-standalone

# Generated BUCK files (from BUILD.dhall) - zero starlark
# All BUCK files are generated, users only edit BUILD.dhall
src/**/BUCK
platforms/BUCK
/BUCK

# GHC build artifacts
*.hi
*.o
*.dyn_hi
*.dyn_o

# LSP generated/symlinked files (root only, not nix/configs/)
/compile_commands.json
/.clangd
/.clang-tidy
/.rustfmt.toml
/.stylua.toml
/hie.yaml
# NOTE: .clang-format is NOT ignored - it's tracked in git for treefmt

# Nix LSP configs (generated)
.nixd.json
.nil.toml
.direnv
.devshell
.cache/
.haskell-sources/
*.gen
src/dice-ffi/target/

# sensenet build output
sensenet-out/

# sensenet toolchains (generated by nix develop)
.sensenet/toolchains.dhall
.sensenet/toolchains.json

# Rust cargo target directories (vendored crates)
src/vendor/*/target/

# PureScript spago cache
.spago/
**/.spago/

# Node modules (never track)
**/node_modules/

# PureScript output (generated)
**/output/
**/output-test/

# sensenet toolchains (generated JSON, keep .dhall)
.sensenet/toolchains.json
